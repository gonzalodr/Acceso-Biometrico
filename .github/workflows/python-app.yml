name: Pruebas Python

on:
  push:
    branches:
      #- main     # Si aún quieres que se ejecute en main
      - gonzalo  # ¡Añade tu rama aquí!

  # pull_request:  # Opción 1: Eliminar completamente la sección pull_request
  #   branches:
  #     - main
  #     - gonzalo

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
    - name: Configurar Python
      uses: actions/setup-python@v5
      with:
        python-version: '3.12' # Asegúrate de que sea la versión correcta

    - name: Instalar dependencias
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Ejecutar Tests con Pytest
      run: |
        pytest src\test_perfil_acceso.py # Asegúrate de la ruta correcta a tu archivo de test